<h1>Listing books</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Author</th>
<!--     <th>Year</th>
    <th>Pages</th>
    <th>Price</th>
    <th>Isbn</th>
    <th>Interview</th>
    <th>Comment</th> -->
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @books.each do |book| %>
  <tr>
    <td><%= book.name %></td>
    <td><%= book.author %></td>
<!--     <td><%= book.year %></td>
    <td><%= book.pages %></td>
    <td><%= book.price %></td>
    <td><%= book.ISBN %></td>
    <td><%= book.interview %></td>
    <td><%= book.comment %></td> -->
    <td><%= link_to 'Show', book %></td>
    <td><%= link_to 'Edit', edit_book_path(book) %></td>
    <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Book', new_book_path %>
